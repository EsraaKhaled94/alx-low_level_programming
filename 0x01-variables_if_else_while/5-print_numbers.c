#include <stdio.h>

/**
 * main - Entry point
 * prints alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number;

	for (number = 0; number < 10 ; number++)
	{
		printf(number);
	}
	putchar('\n');
	return (0);
}
